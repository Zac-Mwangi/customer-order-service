services:
  - type: web
    name: customer-order-service
    env: python
    buildCommand: 
      - pip install -r requirements.txt
      - python manage.py collectstatic --noinput
      - python manage.py migrate
    startCommand: gunicorn CustomerOrderService.wsgi:application
